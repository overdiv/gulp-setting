"use strict";

// babel testing ... 
var path = require('path');

var a = {
  c: c
};
module.exports = {
  ntry: './src/index.js',
  output: {
    filename: 'main.js',
    path: path.resolve(__dirname, 'dist')
  },
  doThis: function doThis(param) {
    param.setState({
      em: em,
      ls: ls
    });
  }
};